import { css } from "@emotion/react";

export const buttonStyle = css({
    border: "none",
    borderRadius: 10,
    backgroundColor: "#1da1f2",
    color: "white",
    ":hover": {
        cursor: "pointer",
    },
});
