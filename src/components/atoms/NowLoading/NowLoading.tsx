/** @jsxImportSource @emotion/react */
import { css } from "@emotion/react";

const NowLoading = () => {
    return <h2 css={css({ textAlign: "center" })}>Now Loading...</h2>;
};

export default NowLoading;
