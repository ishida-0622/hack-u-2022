import { css } from "@emotion/react";

export const tagStyle = css({
    color: "#1da1f2",
    width: "70%",
});

export const showTagStyle = css({ display: "flex", margin: "5px 0" });

export const divStyle = css({
    width: "40%",
    margin: "10px auto",
    textAlign: "left",
});
